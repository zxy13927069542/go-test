package main

/*func main() {
	arr := []int{}	//代表小球
	sums := make(map[int]int)	//存储两数之和
	var sum int
	for i := 0; i < len(arr); i++ {
		for j := 0; j < len(arr); j++ {
			sum = arr[i] + arr[j]
			//将两数之和存入map
			sums[sum] = 1
			//判断m-sum即另外两数之和是否存在
			if _, ok := sums[m - sum]; ok {
				return Yes
			}
		}
	}
	return No
}*/
